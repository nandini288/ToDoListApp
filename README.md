The Java TodoList Application is a simple menu-driven console application developed using Java, MySQL, and JDBC.
This project allows users to manage their daily tasks by adding, viewing, updating, and deleting tasks stored in a MySQL database.

This project demonstrates the implementation of CRUD operations (Create, Read, Update, Delete) using Java and database connectivity.
